import Foundation
import SwiftUI

extension OwnID.UISDK {
    struct AuthButton: View {
        var body: some View {
            Button(action: { }) {
                Image(systemName: "faceid")
            }
        }
    }
}
